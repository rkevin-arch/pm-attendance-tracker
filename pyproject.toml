[project]
name = "pm-attendance-tracker"
version = "0.1.0"
description = "discord bot for tracking meeting attendance"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "discord-py>=2.7.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.10",
]
